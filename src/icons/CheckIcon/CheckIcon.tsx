import React from 'react';
import { SVGComponent } from '../../Icon';

export const CheckIcon: SVGComponent = (forwardedProps) => (
  <svg viewBox="0 0 24 24" {...forwardedProps}>
    <polyline points="20 6 9 17 4 12" />
  </svg>
);
