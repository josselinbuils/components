import React from 'react';
import { IconSVGComponent } from '../../Icon';

export const CheckIcon: IconSVGComponent = (forwardedProps) => (
  <svg focusable="false" viewBox="0 0 24 24" {...forwardedProps}>
    <polyline points="20 6 9 17 4 12" />
  </svg>
);
