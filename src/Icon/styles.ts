import styled from 'styled-components';

export const IconContainer = styled.figure`
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
  margin: 0;

  svg {
    font-size: inherit;
  }
`;
