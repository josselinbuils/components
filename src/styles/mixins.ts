import { fontFamily, fontSize, fontWeight } from './variables';

export const component = `
  font-family: ${fontFamily};
  font-size: ${fontSize};
  font-weight: ${fontWeight};
  line-height: 1.5;
`;
